## Build and release
Run the script with commit message.
```commandline
./deploy.sh "commit message"
```

## Test in local server
1. Start the hugo server with command below.
```commandline
hugo server -D
```
2. Navigate the site at https://localhost:1313/.
